import {
  DEFAULT_DARK_THEME,
  DEFAULT_DARK_THEME_ID,
} from "./default-dark.theme";
import {
  DEFAULT_LIGHT_THEME,
  DEFAULT_LIGHT_THEME_ID,
} from "./default-light.theme";

export {
  DEFAULT_LIGHT_THEME_ID,
  DEFAULT_DARK_THEME_ID,
  DEFAULT_DARK_THEME,
  DEFAULT_LIGHT_THEME,
};
